<Window x:Class="SP26InventoryManagement.Views.CategoryView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Category Management" Height="500" Width="800"
        WindowStartupLocation="CenterScreen">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" BorderBrush="#DDDDDD" BorderThickness="1" CornerRadius="8" Padding="15" Margin="0,0,0,15">
            <StackPanel>
                <TextBlock Text="Category Master Data" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,15"/>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" Margin="0,0,10,0">
                        <TextBlock Text="Category Code (Required):" Margin="0,0,0,5"/>
                        <TextBox Text="{Binding SelectedCategory.CategoryCode, UpdateSourceTrigger=PropertyChanged}" 
                                 TextChanged="InputControl_Changed"
                                 Height="30" VerticalContentAlignment="Center"/>

                        <TextBlock Text="Category Name (Required):" Margin="0,10,0,5"/>
                        <TextBox Text="{Binding SelectedCategory.CategoryName, UpdateSourceTrigger=PropertyChanged}" 
                                 TextChanged="InputControl_Changed"
                                 Height="30" VerticalContentAlignment="Center"/>
                    </StackPanel>

                    <StackPanel Grid.Column="1" Margin="10,0,0,0">
                        <TextBlock Text="Parent Category (Optional):" Margin="0,0,0,5"/>
                        <ComboBox ItemsSource="{Binding Categories}" 
                                  SelectedValue="{Binding SelectedCategory.ParentCategoryId, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                  SelectionChanged="InputControl_Changed"
                                  DisplayMemberPath="CategoryName" 
                                  SelectedValuePath="CategoryId"
                                  Height="30" VerticalContentAlignment="Center"/>

                        <CheckBox Content="Is Active" IsChecked="{Binding SelectedCategory.IsActive}" 
                                  Margin="0,15,0,0" VerticalAlignment="Center"/>

                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                            <Button Content="Refresh" Command="{Binding RefreshCommand}" 
                                    Width="80" Height="32" Margin="0,0,8,0"/>

                            <Button Content="Delete" Command="{Binding DeleteCommand}" 
                                    Width="80" Height="32" Margin="0,0,8,0" Background="#FFE1E1"/>

                            <Button Content="Save" Command="{Binding SaveCommand}" 
                                    Width="100" Height="32" FontWeight="Bold" Background="#E1F5FE" IsDefault="True"/>
                        </StackPanel>
                    </StackPanel>
                </Grid>
            </StackPanel>
        </Border>

        <DataGrid Grid.Row="1" 
                  ItemsSource="{Binding Categories}" 
                  SelectedItem="{Binding SelectedCategory, Mode=TwoWay}"
                  AutoGenerateColumns="False" 
                  IsReadOnly="True" 
                  CanUserAddRows="False"
                  SelectionMode="Single"
                  BorderBrush="#DDDDDD"
                  GridLinesVisibility="Horizontal">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding CategoryId}" Width="50"/>
                <DataGridTextColumn Header="Code" Binding="{Binding CategoryCode}" Width="120"/>
                <DataGridTextColumn Header="Name" Binding="{Binding CategoryName}" Width="*"/>
                <DataGridTextColumn Header="Parent" Binding="{Binding ParentCategory.CategoryName}" Width="150"/>
                <DataGridCheckBoxColumn Header="Active" Binding="{Binding IsActive}" Width="70"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>