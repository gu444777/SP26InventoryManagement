<Window x:Class="SP26InventoryManagement.Views.WarehouseView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Warehouse Management" 
        Height="550" 
        Width="900"
        WindowStartupLocation="CenterScreen">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="WAREHOUSE MANAGEMENT" 
                       FontSize="24" 
                       FontWeight="Bold" 
                       Foreground="#2C3E50"/>
            <Separator Margin="0,5,0,0" Background="#34495E"/>
        </StackPanel>

        <Border Grid.Row="1" 
                Background="#F9F9F9" 
                BorderBrush="#DDD" 
                BorderThickness="1" 
                CornerRadius="5" 
                Padding="15" 
                Margin="0,0,0,20">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Margin="0,0,10,0">
                    <TextBlock Text="Warehouse Code" Margin="0,0,0,5" FontWeight="SemiBold"/>
                    <TextBox Text="{Binding SelectedWarehouse.WarehouseCode, UpdateSourceTrigger=PropertyChanged}" 
                             Height="30" VerticalContentAlignment="Center"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Margin="0,0,10,0">
                    <TextBlock Text="Warehouse Name" Margin="0,0,0,5" FontWeight="SemiBold"/>
                    <TextBox Text="{Binding SelectedWarehouse.WarehouseName, UpdateSourceTrigger=PropertyChanged}" 
                             Height="30" VerticalContentAlignment="Center"/>
                </StackPanel>

                <Button Grid.Column="2" 
                        Content="Save Warehouse" 
                        Command="{Binding SaveCommand}"
                        Width="120" 
                        Height="30" 
                        Margin="0,20,0,0"
                        Background="#27AE60" 
                        Foreground="White" 
                        FontWeight="Bold"/>
            </Grid>
        </Border>

        <GroupBox Grid.Row="2" Header="Warehouse List" FontSize="14">
            <DataGrid ItemsSource="{Binding Warehouses}" 
                      AutoGenerateColumns="False" 
                      CanUserAddRows="False" 
                      IsReadOnly="True"
                      SelectionMode="Single"
                      Margin="5">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding WarehouseId}" Width="50"/>
                    <DataGridTextColumn Header="Code" Binding="{Binding WarehouseCode}" Width="100"/>
                    <DataGridTextColumn Header="Name" Binding="{Binding WarehouseName}" Width="*"/>
                    <DataGridTextColumn Header="Created At" Binding="{Binding CreatedAt, StringFormat=\{0:dd/MM/yyyy HH:mm\}}" Width="130"/>

                    <DataGridTemplateColumn Header="Status" Width="80">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock FontWeight="SemiBold">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsActive}" Value="True">
                                                    <Setter Property="Text" Value="Active"/>
                                                    <Setter Property="Foreground" Value="Green"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding IsActive}" Value="False">
                                                    <Setter Property="Text" Value="Inactive"/>
                                                    <Setter Property="Foreground" Value="Red"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTemplateColumn Header="Actions" Width="220">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <Button Content="Edit" 
                                            Command="{Binding DataContext.EditCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" 
                                            CommandParameter="{Binding}"
                                            Margin="2" Padding="8,2" Background="#3498DB" Foreground="White"/>

                                    <Button Content="Delete" 
                                            Command="{Binding DataContext.DeleteCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" 
                                            CommandParameter="{Binding}"
                                            Margin="2" Padding="8,2" Background="#E74C3C" Foreground="White"/>

                                    <Button Content="Status" 
                                            Command="{Binding DataContext.ToggleStatusCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" 
                                            CommandParameter="{Binding}"
                                            Margin="2" Padding="8,2" Background="#E67E22" Foreground="White"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </GroupBox>
    </Grid>
</Window>